name: ei_simulator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  
  sqflite: ^2.4.3
  path: ^1.9.1
  graphview: ^1.5.1
  provider: ^6.1.5+1
  image_picker: ^1.2.3
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
